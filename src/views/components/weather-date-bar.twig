<div class="bg-[#01012D] text-white">
  <div class="mx-auto flex h-9 w-full max-w-[1400px] items-center justify-between gap-2 px-4 text-xs sm:text-sm">
    <div class="hidden items-center gap-2 sm:flex">
      <span id="vtw-gregorian-date">{{ salla.lang.get('header.weather_bar') }}</span>
      <span>|</span>
      <span id="vtw-hijri-date"></span>
    </div>
    <div class="flex items-center gap-3 truncate">
      <span id="vtw-next-prayer"></span>
      <span id="vtw-weather"></span>
      <span class="truncate">{{ salla.lang.get('header.shipping_note') }}</span>
    </div>
  </div>
</div>
